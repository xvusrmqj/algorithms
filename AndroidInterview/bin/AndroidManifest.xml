<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidinterview"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="15" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:launchMode="singleTask" 
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="components.activity.task.SingleTaskActivity1" />
        <activity android:name="components.activity.task.SingleTaskActivity2" />
        <activity android:name="components.activity.task.SingleTaskActivity3" />
        <activity android:name="components.activity.task.SingleTaskActivity4" />

        <receiver android:name="components.broadcastreciver.CommonReciver" >
            <intent-filter>
                <action android:name="android.intent.action.LVTESTRECEIVER" />
            </intent-filter>
        </receiver>
        <receiver android:name="components.broadcastreciver.OrderedBroadcastReciver" >
            <intent-filter android:priority="100" >
                <action android:name="android.intent.action.LVTESTRECEIVER" />
            </intent-filter>
        </receiver>
    </application>

</manifest>